expr: term (PLUS|MINUS) term
       | UNARY term
term: factor (MULTIPLY|DIVIDE|MODULUS) factor
       | factor '^' factor
       | UNARY factor
factor: NUMBER | VARIABLE | '(' expr ')'
       | UNARY factor

NUMBER: [0-9]+(\.[0-9]+)?
VARIABLE: [a-zA-Z]+
PLUS: '+'
MINUS: '-'
MULTIPLY: '*'
DIVIDE: '/'
MODULUS: '%'
POWER: '^'
UNARY: ('+'|'-')
